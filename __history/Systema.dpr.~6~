program Systema;

uses
  System.StartUpCopy,
  FMX.Forms,
  Unit1principal in 'Unit1principal.pas' {form_principal},
  Unitpvd in 'Views\menu\Unitpvd.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tform_principal, form_principal);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
